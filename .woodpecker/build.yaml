when:
  - event: pull_request
  - event: push
    branch: main

steps:
  - name: restore-cache
    failure: ignore
    image: plugins/cache:1.10@sha256:992cebfcc5eda36ee9862ae1f75efc41be251031f73cb3927895057fe185de9f
    settings:
      access_key:
        from_secret: garage_s3_access_key
      secret_key:
        from_secret: garage_s3_secret_key
      restore: true
      backend: s3
      endpoint: http://garage.garage.svc.cluster.local:3900
      bucket: buildkit-cache
      region: garage
      path_style: true
      archive_format: zstd
      cache_key: "netzbremse-{{ checksum \"go.sum\" }}-{{ checksum \"package-lock.json\" }}"
      mount:
        - .go-mod-cache
        - .npm-cache

  - name: go-test
    image: dhi.io/golang:1.26.2-debian13-dev@sha256:086c893153f92793f3a1541793cd4a8e8b23bfd4ccaf70c8f4261f496080fb0e
    environment:
      GOMODCACHE: /woodpecker/src/github.com/sm-moshi/netzbremse/.go-mod-cache
    commands:
      - mkdir -p .go-mod-cache
      - go test ./...

  - name: browser-script-check
    image: dhi.io/node:25.8.1-debian13-dev@sha256:c2a2fd61528df43f15f31b1650453415e05fe23c910d454559ad5c9456737629
    commands:
      - node --check scripts/netzbremse-browser.mjs

  - name: release
    image: dhi.io/node:25.8.1-debian13-dev@sha256:c2a2fd61528df43f15f31b1650453415e05fe23c910d454559ad5c9456737629
    when:
      - event: push
        branch: main
    environment:
      npm_config_cache: /woodpecker/src/github.com/sm-moshi/netzbremse/.npm-cache
      GITHUB_TOKEN:
        from_secret: github_token
      GH_TOKEN:
        from_secret: github_token
    commands:
      - |
        [ -n "$APT_PROXY_HOST" ] && echo "Acquire::http::Proxy \"http://$APT_PROXY_HOST:${APT_PROXY_PORT:-3142}\";" > /etc/apt/apt.conf.d/00proxy
      - apt-get update
      - apt-get install -y --no-install-recommends git ca-certificates
      - rm -rf /var/lib/apt/lists/*
      - rm -f .git/config.lock
      - git config user.name "woodpecker[bot]"
      - git config user.email "woodpecker@m0sh1.cc"
      - git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/sm-moshi/netzbremse.git"
      - git fetch --tags origin
      - npm ci
      - node node_modules/.bin/semantic-release

  - name: rebuild-cache
    failure: ignore
    image: plugins/cache:1.10@sha256:992cebfcc5eda36ee9862ae1f75efc41be251031f73cb3927895057fe185de9f
    settings:
      access_key:
        from_secret: garage_s3_access_key
      secret_key:
        from_secret: garage_s3_secret_key
      rebuild: true
      backend: s3
      endpoint: http://garage.garage.svc.cluster.local:3900
      bucket: buildkit-cache
      region: garage
      path_style: true
      archive_format: zstd
      cache_key: "netzbremse-{{ checksum \"go.sum\" }}-{{ checksum \"package-lock.json\" }}"
      mount:
        - .go-mod-cache
        - .npm-cache
